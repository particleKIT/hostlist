---

language: python
python:
  - 3.4
  - 3.5
  - 3.6
install:
  - pip install -r requirements.txt
  - pip install flake8 flit nose
  # - pip install pylint
build:
  ci:
    - flake8 hostlist
    # - pylint hostlist
    - nosetests tests
    - flit wheel
