---

language: python
python:
  - 3.3
  - 3.4
  - 3.5
install:
  - pip install -r requirements.txt
  - pip install flake8 flit pylint nose
build:
  ci:
    - flake8 hostlist
    - pylint hostlist
    - nosetests tests
    - flit wheel
